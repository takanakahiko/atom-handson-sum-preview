'use babel';

import AtomHandsonSumPreviewView from '../lib/atom-handson-sum-preview-view';

describe('AtomHandsonSumPreviewView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
